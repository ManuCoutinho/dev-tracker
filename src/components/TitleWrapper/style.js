import styled from 'styled-components';

export const Header = styled.header `
  grid-column: 3 / -3; 
  h1 {
    font-family: 'Unica One', cursive;
    font-size: 3rem;
    line-height: 4rem;
    margin-left: -2.5rem;  
  }

`